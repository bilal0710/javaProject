CREATE TABLE PERSON (ID BIGINT IDENTITY NOT NULL, CREATED TIMESTAMP, EMAIL VARCHAR, FIRSTNAME VARCHAR, LASTNAME VARCHAR, MODIFIED TIMESTAMP, VERSION BIGINT, ADDRESSBOOK_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE ADDRESSBOOK (ID BIGINT IDENTITY NOT NULL, CREATED TIMESTAMP, MODIFIED TIMESTAMP, VERSION BIGINT, PRIMARY KEY (ID))
CREATE TABLE ADDRESS (ID BIGINT IDENTITY NOT NULL, CITY VARCHAR, CREATED TIMESTAMP, MODIFIED TIMESTAMP, STREET VARCHAR, VERSION BIGINT, ZIPCODE VARCHAR, PRIMARY KEY (ID))
ALTER TABLE PERSON ADD CONSTRAINT FK_PERSON_ADDRESSBOOK_ID FOREIGN KEY (ADDRESSBOOK_ID) REFERENCES ADDRESSBOOK (ID)
